<% content_for :title,'群认领验证码' %><div class="narrow-page"><h1><%= @claim.used_at ? '这座群馆，认领成功。' : '把这张认领条，带回群里' %></h1><% if @token %><p>在你管理的 QQ 群中，发送：</p><div class="claim-code mono">@机器人 <%= @token %></div><p>有效至 <%= museum_time(@claim.expires_at) %>。认领码只在这里完整显示一次。</p><% elsif @claim.used_at %><p><%= group_link(@claim.group) %> 已与你的网站账号建立管理关系。</p><% else %><div class="privacy-note">原始认领码不会再次显示。若已丢失，请生成新的认领码。</div><% end %><%= link_to '返回认领记录',claims_path,class:'button button-ghost' %><% if AppConfig.demo.enabled && @token %><div class="section"><h2>在虚拟 QQ 群演示认领</h2><p class="muted">这里仍然会检查模拟成员的真实角色。选择普通成员会失败。</p><%= form_with url:demo_actions_path,class:'stack-form' do |f| %><%= f.hidden_field :action_name,value:'claim' %><%= f.hidden_field :token,value:@token %><label>虚拟群<%= f.select :group_id,Group.order(:id).map { |g|[g.display_name,g.id] } %></label><label>发送者<%= f.select :transporter_id,Transporter.order(:id).limit(36).map { |t|[t.display_name,t.id] } %></label><%= f.submit '模拟 @机器人 发送认领码',class:'button' %><% end %></div><% end %></div>
