<% content_for :title,'举报或申请处理' %><div class="narrow-page"><span class="mono"><%= @entry.sid %></span><h1>这件藏品，需要处理</h1><p>如果包含隐私、伤害内容，或是你本人的内容，请告诉我们。</p><%= render 'shared/errors',record:@report %><%= form_with model:@report,url:entry_reports_path(@entry),class:'stack-form' do |f| %><label>处理原因<%= f.select :reason,Report::REASONS.map { |k,v|[v,k] },{prompt:'请选择原因'},required:true %></label><label>补充说明<%= f.text_area :details,rows:5,maxlength:3000,placeholder:'描述需要处理的部分。请不要在这里补充他人的私人信息。' %></label><%= f.submit '提交处理请求',class:'button' %><% end %><p class="small muted">提交记录和处理结果可以在你的个人主页查看。</p></div>
